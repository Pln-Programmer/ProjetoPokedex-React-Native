import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    header: {
        marginBottom: 25
    },

    container: {
        flex: 1,
        alignItems: 'center',
        gap: 25
    }
})

export default styles